

import type { 
  Agent,
  Post
} from "../../api";
import {
  makeApiCall,
  makeApiInfoCall,
  REMOTE_ORIGIN
} from "../../api";

export async function getOutstandingAgents(
    setAgents: (a: { [key: string]: Agent }) => void
){

}

